package swt6.football.web.util;

import java.util.Date;

import org.springframework.stereotype.Component;

@Component
public class DateToClassConverter {

	public String calculateClass(Date date) {
		return date.after(new Date()) ? "warning" : "success";
	}
}
