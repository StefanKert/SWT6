<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">

<head th:replace="layout :: head"><title>App Title</title></head>

<body role="document">
<div th:replace="layout :: navBar">Navigation Bar</div>

<div class="container">

    <div class="page-title">
        <h2 th:text="#{games.title}">Games List</h2>
    </div>

		<table class="table table-striped table-condensed">
			<thead>
				<tr>
					<th>Team A</th>
					<th>Team B</th>
					<th>Goals - Team A</th>
					<th>Goals - Team B</th>
					<th>Date</th>
					<th>Set Bet</th>
					<th>Update</th>
				</tr>
			</thead>
			<tbody>
				<tr th:each="game : ${games}" >
					<td th:text="${game.teamA.name}">Team A</td>
					<td th:text="${game.teamB.name}">Team B</td>
					<td th:text="${game.goalsA}">0</td>
					<td th:text="${game.goalsB}">0</td>
					<td th:text="${game.date}">01-01-01</td>
					<td>
						<a th:href="@{|/bets/${game.id}/create|}">Set Bet</a>
					</td>
					<td>
						<a th:href="@{|/games/${game.id}|}">Edit</a>
					</td>
				</tr>
			</tbody>
		</table>
    <a th:href="@{/games/create}" th:text="#{games.create}">games</a>


</div>
</body>
</html>